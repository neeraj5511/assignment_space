<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section>
      <h1>
        <%= @user.name %>
      </h1>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @user.blog_posts.any? %>
      <h3>Blogposts (<%= @user.blog_posts.count %>)</h3>
      <ol class="microposts">
        <%= render @blogposts %>
      </ol>
    <% end %>
  </div>
  <div class="col-md-8">
    <%= render 'follow_form' if current_user %>
  </div>
</div>